# htllm

## Vision
Claudeが説明する内容ごとに、その場で専用の動的Webページを生成して見せる。
Claudeの計画・成果物をわかりやすく表現して正しく理解する。
成果物・計画をインタラクティブに人が修正できる。
わからないところをすぐ聞ける。

## 具体例
- データの流れや動きを追える。
- 動的コンテンツでわかりやすい。
- Notionのコメントのように、コメントを作るとClaudeが答えてくれる。
- 簡単に修正でき、その修正をすぐに反映できる。
  例: structに新しいメンバをWebUIから追加でき、それを即座に反映できる。

## 開発ループ

1. 仮説ベースでロードマップ作成
2. ADR作成＋専門家AIレビュー
3. 仕様策定→テスト設計→設計→実装→検証
4. ベンチ/検証

1-4を繰り返す

## 技術方針
- 動的ページ生成: React。ただしビルドレス（CDN経由のReact + その場トランスパイル）で、生成のたびにbundlerを挟まない。
- Claude Code連携: headlessモード（`claude -p --output-format stream-json`）を`claude/runTurn.ts`からspawnする。`type: "result"`の行からresultとsession_idを取り出し、`--resume`でセッションを継続する。zellij常駐案とAgent SDKは不採用。

## スレッド応答の実装ルール
- スレッドへの返信は毎回、`render.ts`の`respond()`を1回だけ呼び出して処理する。
- `respond()`へのプロンプトでは、LLMに応答の1行目へ`ANSWER`（続けて質問への回答文）か`EDIT`（続けて書き換え後のフィールドJSON）のどちらかを書かせ、その1回の応答結果だけを見て「回答を表示する」か「部品を書き換える」かを決める。
- 同じスレッド内でも、ある返信では回答し、別の返信では書き換える、というように毎回`respond()`の結果に従って個別に振る舞いを決める。
- EDITの場合、返ってきたフィールドで対象Nodeを置き換えて`doc.json`に保存する。これによりページの表示内容そのものが変わり、同じ部品に紐づく他スレッドのハイライト範囲(range)は無効化(null)される。

## Git / GitHub運用

### ブランチ
- mainブランチ上で直接コミット・編集はしない。どんな作業も必ず`feat/xxx`ブランチを切ってから始める。「直前の作業をmainにマージした続き」であっても例外にしない。
- worktreeは使わない（単一開発者・単一ストリームのため。並列で複数の実験を試したい具体的な場面が出たときだけ再検討する）。

### コミット
- TDDサイクル（Red → Green → Refactor）が1周し、テストがGreenになったタイミングで確認なしにコミットする。
- コミットメッセージはConventional Commits形式（`feat:`, `fix:`, `refactor:`, `test:`, `docs:`など）。本文は日本語で可。

### push / PR
- `feat/xxx`ブランチの作業はリモートにpushし、GitHub PRを作成してmainに反映する。
- PRがマージされたら、feature branchはリモート・ローカルとも削除する。
- `main`への直接push、force push、コミット履歴の書き換えは行わない。

## 最初のマイルストーン
静的HTML生成→ローカルサーバー配信→ブラウザ表示、の最小ループを作る。zellij連携・自動検知は含めない。
